node_modules
dist
dist-ssr
*.local

# Docs playground: built from the app by `npm run build:playground` (and by CI
# before each Pages deploy). A generated artifact — not checked in.
docs/static/playground/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Internal brand/product spec — not tracked
PRODUCT.md

# TypeScript composite build output (from `tsc -b`)
*.tsbuildinfo
vite.config.js
vite.config.d.ts

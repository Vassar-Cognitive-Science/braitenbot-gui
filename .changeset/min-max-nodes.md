---
"braitenbot-gui": minor
---

Add Minimum and Maximum compute nodes (Advanced palette). Each reduces over its incoming weighted signals — `min`/`max` respectively — enabling winner/loser-take-all wiring and constant-based floors/ceilings that summation and multiply can't express. Faithful in both the trace simulator and generated Arduino code.

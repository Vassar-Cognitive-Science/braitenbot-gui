---
"braitenbot-gui": patch
---

The kit photocell presets (Left/Right Photocell on the Basic tab) now drop with "Invert signal" enabled, so new users get the expected more light → more signal behavior for simple vehicles. The photocells are wired as voltage dividers where the raw reading drops as light increases; the checkbox can still be unticked for the raw reading.
